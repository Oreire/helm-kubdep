apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coweb.fullname" . }}-test-connection"
  labels:
    {{- include "coweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
