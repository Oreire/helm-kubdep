apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}  # Uses the correct value from values.yaml
  labels:
    app: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      {{- if eq .Values.service.type "ClusterIP" }}
      clusterIP: {{ .Values.service.clusterIP }}
      {{- end }}
  selector:
    app: {{ .Release.Name }}

