apiVersion: v1
kind: Service
metadata:
  name: {{ include "coweb.fullname" . }}
  labels:
    {{- include "coweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coweb.selectorLabels" . | nindent 4 }}
